.data
backn: .asciiz "\n"
.text
.globl main

mamad:

    add $t0,$a0,$a1
    move $s0,$t0
    
soheil:

hasan:

main:

    add $t2,$t0,$t1
    move $s1,$t2
    mul $t3,$t1,$t2
    add $t4,$t0,$t3
    div $t4,$t5
    move $t6,$lo
    move $s2,$t6
    mul $t1,$s2,$t0
    add $t2,$s1,$t1
    move $s3,$t2
    move $a3,$t3
    move $a2,$t2
    move $a1,$t1
    move $a0,$t0
